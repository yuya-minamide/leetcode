// function largestNumber(nums: number[]): string {
//   const SortedNums = nums.sort((a, b) => Number(`${b}${a}`) - Number(`${a}${b}`)).join('');

//   return SortedNums.startsWith('0') ? '0' : SortedNums;
// };
